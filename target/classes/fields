    private int id; //Значение поля должно быть больше 0, Значение этого поля должно быть уникальным, Значение этого
    // поля должно генерироваться автоматически
    private String name; //Поле не может быть null, Строка не может быть пустой
    private Coordinates coordinates; //Поле не может быть null
    private java.util.Date creationDate; //Поле не может быть null, Значение этого поля должно генерироваться автоматически
    private float salary; //Значение поля должно быть больше 0
    private java.time.LocalDate startDate; //Поле не может быть null
    private Position position; //Поле не может быть null
    private Status status; //Поле может быть null
    private Organization organization; //Поле может быть null

    id, name, coordinates, creationDate, salary,
    startDate(we'll set it as birthday),
    position(manager, laborer, baker),
    status(hired, fired, regular),
    organization

    name, coordinates, creationDate, salary, startDate, position, status, organization


    private String fullName; //Поле не может быть null
    private Integer annualTurnover; //Поле может быть null, Значение поля должно быть больше 0
    private int employeesCount; //Значение поля должно быть больше 0

var line = console.readln().trim();
while (line.isEmpty) {
	if (line.equals("exit")) throw new AskExitException;
	if (!line.isEmpty()) {
		...
	}
}



 public Worker(String name, Coordinates coordinates, java.util.Date creationDate, float salary,
                  java.time.LocalDate startDate, Position position, Status status, Organization organization)





 id, name, organization, position, status, salary, coordinates, creationDate, 
    startDate(we'll set it as a birthday)
    
    